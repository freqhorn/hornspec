ansible_user: Administrator
ansible_password: windowsisfun400!
ansible_port: 5985
ansible_connection: winrm
# The following is necessary for Python 2.7.9+ (or any older Python that has
# backported SSLContext, eg, Python 2.7.5 on RHEL7) when using default WinRM
# self-signed certificates:
ansible_winrm_server_cert_validation: ignore
ansible_become_method: runas
ansible_become_user: Administrator
ansible_become_password: windowsisfun400!

cygwin_root: C:\tools\cygwin